package com.tlfmj.bao.sampletwo.activity;

import com.mj.utils.receiver.MyReceiver;

/**
 * 自定义接收器
 * <p>
 * 如果不定义这个 Receiver，则：
 * 1) 默认用户会打开主界面
 * 2) 接收不到自定义消息
 */
public class MyStartReceiver extends MyReceiver {
    private static final String TAG = "JIGUANG-Example";


}
